{
  "name": "omnimap-agent",
  "version": "1.0.0",
  "description": "OmniMap Agent — map data extraction and analysis bot for Telegram (Python FastAPI + Supabase)",
  "private": true,
  "engines": {
    "node": ">=18.18"
  },
  "scripts": {
    "prisma:generate": "prisma generate",
    "prisma:migrate": "sh -c '. ./.env && DATABASE_URL=\"$DIRECT_URL\" prisma migrate dev'",
    "prisma:deploy": "sh -c '. ./.env && DATABASE_URL=\"$DIRECT_URL\" prisma migrate deploy'",
    "prisma:studio": "prisma studio"
  },
  "devDependencies": {
    "@prisma/client": "^7.1.0",
    "dotenv": "^16.6.1",
    "prisma": "^7.1.0"
  },
  "keywords": [
    "telegram",
    "bot",
    "fastapi",
    "python",
    "supabase"
  ],
  "author": "",
  "license": "MIT"
}
